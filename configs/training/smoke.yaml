name: smoke_mlp_v2
dataset_dir: artifacts/datasets/smoke_v2
output_dir: artifacts/runs
seed: 42
device: auto
batch_size: 256
epochs: 40
patience: 8
learning_rate: 0.001
weight_decay: 0.0001
hidden_sizes: [128, 128, 128]
classifier_hidden_sizes: [64, 64]
dropout: 0.1
classifier_dropout: 0.1
