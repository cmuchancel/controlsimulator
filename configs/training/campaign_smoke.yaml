name: campaign_smoke
dataset_dir: artifacts/datasets/campaign_smoke
output_dir: artifacts/runs
seed: 7
device: cpu
batch_size: 64
epochs: 3
patience: 2
learning_rate: 0.001
weight_decay: 0.0001
hidden_sizes: [64, 64, 64, 64]
classifier_hidden_sizes: [64, 64, 64, 64]
dropout: 0.0
classifier_dropout: 0.0
feature_set: campaign_core
activation: relu
classifier_activation: relu
